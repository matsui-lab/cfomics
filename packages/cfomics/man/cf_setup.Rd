% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env_setup.R
\name{cf_setup}
\alias{cf_setup}
\title{One-Command Environment Setup for cfomics}
\usage{
cf_setup(
  level = c("standard", "minimal", "full"),
  gpu = FALSE,
  force = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{level}{Setup level: "minimal" (R-only), "standard" (common methods),
or "full" (all methods including deep learning)}

\item{gpu}{Logical, whether to install GPU-enabled packages (TensorFlow, PyTorch)}

\item{force}{Logical, whether to recreate environments if they exist}

\item{verbose}{Logical, whether to show progress messages}
}
\value{
Invisibly returns TRUE on success
}
\description{
Sets up the complete Python environment for cfomics with a single command.
Automatically installs Miniconda if needed and creates the appropriate
conda environment with all dependencies.
}
\examples{
\dontrun{
cf_setup()                      # Standard setup
cf_setup(level = "minimal")     # R-only, no Python
cf_setup(level = "full", gpu = TRUE)  # Everything with GPU
}
}
