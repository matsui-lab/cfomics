% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reporting.R
\name{cf_export}
\alias{cf_export}
\title{Export Results to File}
\usage{
cf_export(fit, file, format = NULL, include_ite = TRUE, overwrite = FALSE)
}
\arguments{
\item{fit}{A cf_model object from cf_fit()}

\item{file}{Output file path}

\item{format}{File format: auto-detected from extension, or specify
"csv", "rds", "json"}

\item{include_ite}{Logical, whether to include individual treatment effects}

\item{overwrite}{Logical, whether to overwrite existing files. If FALSE
(default), an error is raised when the file already exists.}
}
\value{
Invisibly returns the file path
}
\description{
Exports causal inference results to various file formats.
}
\examples{
# Fit a model
set.seed(123)
demo_data <- data.frame(
  X = rnorm(30),
  T = rbinom(30, 1, 0.5)
)
demo_data$Y <- 2 * demo_data$T + rnorm(30)
fit <- cf_fit(Y ~ T | X, data = demo_data, method = "gformula")

# Export to temporary file (RDS format - recommended for R)
tmp_file <- tempfile(fileext = ".rds")
cf_export(fit, tmp_file, format = "rds")
unlink(tmp_file)  # Clean up

\donttest{
# Export to CSV (creates two files: data and summary)
tmp_csv <- tempfile(fileext = ".csv")
cf_export(fit, tmp_csv, format = "csv")
unlink(tmp_csv)
unlink(sub("\\\\.csv$", "_summary.csv", tmp_csv))
}
}
